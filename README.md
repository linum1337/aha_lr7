# Лабораторная работа №7: Кучи

**Студент:** Цехов Владислав Федорович 
**Группа:** ПИЖ-б-о-23-1 
**Направление:** Программная инженерия
**Дисциплина:** Анализ сложности алгоритмов  
---

## Цель работы

Изучить структуру данных "куча" (heap), её свойства и применение. Освоить основные операции с кучей (добавление, извлечение корня) и алгоритм её построения. Получить практические навыки реализации кучи на основе массива (array-based), исследовать эффективность основных операций и применение кучи для сортировки и реализации приоритетной очереди.

---

## Характеристики ПК для тестирования

- **Операционная система:** WIN11
- **Процессор:** AMD Ryzen 5 3600
- **Python:** 3.14
- **Память:** 16 GB DDR4
- **Лимит рекурсии:** 10000

> **Важно:** Все замеры проведены на одной вычислительной машине для обеспечения корректности сравнения.

---

## Структура проекта

```
lab07_heap/
├── heap.py                  # Реализация классов Heap, MinHeap, MaxHeap
├── heapsort.py             # Алгоритм сортировки кучей
├── priority_queue.py        # Приоритетная очередь на основе кучи
├── analysis_heap.py        # Экспериментальное исследование
├── test_heap.py            # Unit-тесты
└── README.md               # Отчет (этот файл)
```

---

## Теоретическая часть

### Куча (Heap)

**Определение:** Специализированная древовидная структура данных, которая является полным бинарным деревом (все уровни заполнены, кроме последнего, который заполняется слева направо) и удовлетворяет свойству кучи.

### Свойства кучи

#### Min-Heap (минимальная куча)
- Значение в любом узле **≤** значениям его потомков
- Корень содержит **минимальный** элемент
- Используется для извлечения минимума

#### Max-Heap (максимальная куча)
- Значение в любом узле **≥** значениям его потомков
- Корень содержит **максимальный** элемент
- Используется для извлечения максимума

### Представление в виде массива

Куча эффективно реализуется на основе массива. Для узла с индексом `i`:

```
Индекс родителя:      (i - 1) // 2
Индекс левого потомка:  2*i + 1
Индекс правого потомка: 2*i + 2
```

### Временная сложность операций

| Операция | Временная сложность | Пространственная сложность |
|----------|--------------------|-----------------------------|
| insert() | O(log n) | O(1) амортизированная |
| extract() | O(log n) | O(1) |
| peek() | O(1) | O(1) |
| build_heap() | **O(n)** | O(1) |
| Heapsort | O(n log n) | O(1) - in-place |

---

## Реализация

### 1. Класс Heap (heap.py)

#### Основные методы:

**insert(value)** - Вставка элемента
- Временная сложность: **O(log n)**
- Алгоритм: добавляем в конец, затем "всплываем" (sift-up)

**extract()** - Извлечение корня (мин/макс)
- Временная сложность: **O(log n)**
- Алгоритм: берем корень, ставим последний элемент на место корня, "погружаем" (sift-down)

**peek()** - Просмотр корня
- Временная сложность: **O(1)**
- Просто возвращаем первый элемент

**build_heap(array)** - Построение кучи из массива
- Временная сложность: **O(n)** (а не O(n log n)!)
- Алгоритм: начинаем с последнего родительского узла и идем к корню, вызывая sift-down

#### Вспомогательные методы:

**_sift_up(index)** - Всплытие элемента
- Временная сложность: O(log n)
- Поднимаем элемент вверх до восстановления свойства кучи

**_sift_down(index)** - Погружение элемента
- Временная сложность: O(log n)
- Опускаем элемент вниз до восстановления свойства кучи

**is_valid_heap()** - Проверка корректности
- Временная сложность: O(n)
- Проверяем все узлы на соответствие свойству кучи

**visualize()** - Текстовая визуализация
- Выводит кучу в виде дерева с отступами

### Пример использования:

```python
from heap import MinHeap

# Создание и вставка
heap = MinHeap()
heap.insert(50)
heap.insert(30)
heap.insert(70)

# Просмотр минимума
print(heap.peek())  # 30

# Извлечение минимума
print(heap.extract())  # 30
print(heap.extract())  # 50
print(heap.extract())  # 70

# Построение из массива
array = [50, 30, 70, 20, 40]
heap2 = MinHeap()
heap2.build_heap(array)  # O(n), быстрее чем 5 insert
```

### 2. Сортировка кучей (heapsort.py)

#### heapsort(array)
- Использует дополнительную память для max-heap
- Временная сложность: **O(n log n)**
- Пространственная сложность: **O(n)**

Алгоритм:
1. Строим max-heap из массива
2. Последовательно извлекаем максимум
3. Добавляем в результат
4. Переворачиваем результат

#### heapsort_inplace(array)
- **Modifies array in-place** - без дополнительной памяти
- Временная сложность: **O(n log n)**
- Пространственная сложность: **O(1)**

Алгоритм:
1. Преобразуем массив в max-heap
2. Берем корень (максимум) и ставим в конец
3. Уменьшаем размер кучи
4. Восстанавливаем свойство
5. Повторяем

### Пример использования:

```python
from heapsort import heapsort, heapsort_inplace

array = [64, 34, 25, 12, 22, 11, 90]

# С дополнительной памятью
sorted_array = heapsort(array)
print(sorted_array)  # [11, 12, 22, 25, 34, 64, 90]

# In-place
array2 = [64, 34, 25, 12, 22, 11, 90]
heapsort_inplace(array2)
print(array2)  # [11, 12, 22, 25, 34, 64, 90]
```

### 3. Приоритетная очередь (priority_queue.py)

#### PriorityQueue (на основе min-heap)
- Элементы с меньшим приоритетом извлекаются первыми
- Используется для задач с приоритетами

#### MaxPriorityQueue (на основе max-heap)
- Элементы с большим приоритетом извлекаются первыми

#### Методы:

**enqueue(item, priority)** - Добавление элемента
- Временная сложность: **O(log n)**

**dequeue()** - Извлечение элемента с наивысшим приоритетом
- Временная сложность: **O(log n)**

**peek()** - Просмотр элемента с наивысшим приоритетом
- Временная сложность: **O(1)**

### Пример использования:

```python
from priority_queue import PriorityQueue

pq = PriorityQueue()

# Добавление задач с приоритетами
pq.enqueue("Критичное", 1)      # Высокий приоритет (1)
pq.enqueue("Обычное", 5)        # Низкий приоритет (5)
pq.enqueue("Важное", 2)         # Средний приоритет (2)

# Извлечение в порядке приоритета
print(pq.dequeue())  # "Критичное" (приоритет 1)
print(pq.dequeue())  # "Важное" (приоритет 2)
print(pq.dequeue())  # "Обычное" (приоритет 5)
```

---

## Экспериментальное исследование

### 1. Построение кучи: Последовательные вставки vs build_heap

#### Методология

- **Метод 1:** Последовательные вставки - вызываем insert() n раз
  - Сложность: O(n log n)
  - Каждая вставка добавляет элемент и всплывает

- **Метод 2:** build_heap() - преобразуем массив в кучу алгоритмом heapify
  - Сложность: **O(n)** - почему меньше, чем O(n log n)?
  - Большинство элементов находятся внизу дерева и всплывают на короткие расстояния
  - Математически: сумма высот всех узлов = O(n)

#### Результаты замеров

| Размер | Послед. вставка (с) | build_heap (с) | Ускорение |
|--------|---------------------|----------------|-----------|
| 1 000 | 0.000595 | 0.000474 | **1.25x** |
| 5 000 | 0.003454 | 0.002678 | **1.29x** |
| 10 000 | 0.006306 | 0.004611 | **1.37x** |
| 20 000 | 0.012882 | 0.009038 | **1.43x** |

#### Выводы

1. **build_heap всегда быстрее**, даже на малых размерах
2. **Ускорение растет** с увеличением размера (до 1.43x на 20k)
3. **Асимптотическое различие видно:** build_heap растет медленнее O(n) vs O(n log n)
4. **Практический совет:** Если нужно создать кучу из готового массива - используйте build_heap!

---

### 2. Тестирование Min-Heap

#### Исходные данные

Вставляемые значения: [50, 30, 70, 20, 40, 60, 80, 10]

#### Результаты

**Массив кучи после вставок:** [10, 20, 60, 30, 40, 70, 80, 50]

**Визуализация дерева:**
```
                    10
                   /  \
                  20   60
                 / \   / \
                30 40 70 80
               /
              50
```

**Извлечение элементов (in-order):** [10, 20, 30, 40, 50, 60, 70, 80]

**Проверка свойства:** Куча валидна (каждый родитель ≤ детей)

---

### 3. Сравнение алгоритмов сортировки

#### Методология

Сравниваем 4 алгоритма:
1. **Heapsort** (in-place) - наша реализация
2. **QuickSort** - быстрая сортировка
3. **MergeSort** - сортировка слиянием
4. **Timsort** - встроенная сортировка Python

#### Результаты замеров

| Размер | Heapsort | QuickSort | MergeSort | Timsort | Лучший |
|--------|----------|-----------|-----------|---------|--------|
| 1 000 | 0.001521 | 0.001550 | 0.001565 | **0.000102** | Timsort |
| 5 000 | 0.009729 | **0.005183** | 0.009340 | **0.000555** | Timsort |
| 10 000 | 0.021980 | **0.012614** | 0.021998 | **0.001203** | Timsort |

#### Анализ результатов

**1. Timsort значительно быстрее**
- Встроенная сортировка Python использует Timsort (гибрид MergeSort + InsertionSort)
- Оптимизирован под реальные данные
- В 10-20 раз быстрее наших реализаций на Python

**2. QuickSort лучше Heapsort и MergeSort**
- QuickSort в среднем случае быстрее на 40-50%
- Причина: лучшая локальность кэша, меньше операций перемещения

**3. Heapsort сопоставим с MergeSort**
- Оба имеют гарантированный O(n log n)
- Heapsort использует O(1) памяти (in-place)
- MergeSort требует O(n) доп. памяти

**4. Практический вывод**
- **Для production:** используйте встроенную sort() или sorted()
- **Для учебных целей:** QuickSort в среднем лучше Heapsort
- **Если важна память:** Heapsort (in-place, O(1) доп. памяти)
- **Если важна гарантия:** Heapsort или MergeSort (O(n log n) в худшем случае)

---

### 4. Тестирование приоритетной очереди

#### Пример использования

```python
pq = PriorityQueue()
pq.enqueue("Задача A", 5)
pq.enqueue("Задача B", 1)
pq.enqueue("Задача C", 3)
```

#### Результат

**Порядок извлечения:** [Задача B, Задача C, Задача A]

Задачи извлекаются в порядке возрастания приоритета:
- Задача B (приоритет 1) - первой
- Задача C (приоритет 3) - второй
- Задача A (приоритет 5) - третьей

---

## Тестирование

### Unit-тесты для проверки:

```python
# Тест 1: Min-Heap
heap = MinHeap()
heap.insert(50)
heap.insert(30)
assert heap.peek() == 30  # Минимум
assert heap.extract() == 30
assert heap.is_valid_heap() == True

# Тест 2: Max-Heap
max_heap = MaxHeap()
for val in [50, 30, 70]:
    max_heap.insert(val)
assert max_heap.peek() == 70  # Максимум

# Тест 3: build_heap
array = [50, 30, 70, 20]
heap = MinHeap()
heap.build_heap(array)
assert heap.is_valid_heap() == True

# Тест 4: Heapsort
result = heapsort([64, 34, 25, 12, 22, 11, 90])
assert result == [11, 12, 22, 25, 34, 64, 90]

# Тест 5: In-place Heapsort
array = [64, 34, 25, 12, 22, 11, 90]
heapsort_inplace(array)
assert array == [11, 12, 22, 25, 34, 64, 90]

# Тест 6: PriorityQueue
pq = PriorityQueue()
pq.enqueue("A", 5)
pq.enqueue("B", 1)
assert pq.dequeue() == "B"
assert pq.dequeue() == "A"
```

### Запуск тестов:

```bash
python -m pytest test_heap.py -v
# или
python test_heap.py
```

---

## Выводы

### 1. Теоретическая сложность подтверждается практикой

**build_heap O(n) vs insert O(n log n):**
- Ускорение растет с размером данных
- На 20k элементах: 1.43x быстрее
- Это следствие того, что большинство элементов находятся внизу дерева

### 2. Куча имеет специфические применения

**Эффективна для:**
-  Сортировки когда нужна гарантия O(n log n) в худшем случае
-  Приоритетных очередей (самое частое применение)
-  Поиска k наибольших/наименьших элементов
-  Когда критична O(1) доп. памяти (in-place)

**Менее эффективна для:**
-  Общей сортировки (Timsort лучше в 10+ раз)
-  Когда необходима хорошая локальность кэша
-  Когда данные частично отсортированы

### 3. Приоритетная очередь - основное практическое применение

**Используется в:**
- Алгоритме Дейкстры (поиск кратчайшего пути)
- Алгоритме Прима (минимальное остовное дерево)
- Операционных системах (планирование задач)
- Симуляциях и системах с событиями

### 4. Array-based представление - правильный выбор

**Преимущества vs pointer-based:**
-  Экономия памяти (не нужны указатели)
-  Лучше локальность кэша (элементы подряд)
-  Проще реализация (формулы для родителя/детей)
-  Быстрее операции

### 5. In-place Heapsort - важное свойство

- **O(1) дополнительной памяти** (только рабочие переменные)
- Может применяться когда памяти критически мало
- Хотя на практике QuickSort часто быстрее

---

##  Ответы на контрольные вопросы

### 1. Сформулируйте основное свойство min-кучи и max-кучи

**Min-куча:** Для каждого узла **значение узла ≤ значений его потомков**. Корень содержит минимальный элемент дерева.

**Max-куча:** Для каждого узла **значение узла ≥ значений его потомков**. Корень содержит максимальный элемент дерева.

**Пример min-кучи:**
```
        10            ← минимум в корне
       /  \
      20   60
     / \   / \
    30 40 70 80
```

---

### 2. Опишите алгоритм операции вставки (sift_up)

**Алгоритм вставки в min-кучу:**

1. **Добавляем элемент в конец массива** - это сохраняет свойство полного бинарного дерева
2. **Всплываем (sift-up):**
   - Пока элемент не в корне И нарушено свойство кучи:
     - Сравниваем с родителем
     - Если элемент < родителя (для min-heap):
       - Меняем местами
       - Переходим к родителю
     - Иначе: свойство восстановлено, выходим

**Пример:** Вставляем 5 в кучу [10, 20, 60, 30, 40, 70, 80]
```
Шаг 1: Добавляем в конец: [10, 20, 60, 30, 40, 70, 80, 5]
Шаг 2: 5 < 40 (родитель), меняем: [10, 20, 60, 30, 5, 70, 80, 40]
Шаг 3: 5 < 20 (родитель), меняем: [10, 5, 60, 30, 20, 70, 80, 40]
Шаг 4: 5 < 10 (родитель), меняем: [5, 10, 60, 30, 20, 70, 80, 40]
Шаг 5: 5 в корне, стоп. ✓
```

**Сложность:** O(log n) - максимум h уровней дерева

---

### 3. Почему build_heap равна O(n), а не O(n log n)?

**Неправильно думать:** "Вызываем sift-down для n/2 узлов, каждый вызов O(log n), значит O(n log n)"

**Правильный анализ:** Нужно считать **сумму глубин узлов при погружении**

**Ключевое понимание:** 
- На глубине d находится ≈ n/2^d узлов
- Каждый узел на глубине d нуждается максимум в (h-d) операциях sift-down
- Узлы внизу дерева - большинство, но всплывают на короткие расстояния

**Математическое доказательство:**

```
Сумма операций = Σ(количество узлов на глубине i × расстояние до листа)
               = Σ(n/2^i × (h-i))
               = n × Σ((h-i)/2^i)
               = n × (константа < 2)
               = O(n)
```

**Практический смысл:**
- 50% узлов - листья (не всплывают вообще)
- 25% узлов - всплывают на 1 уровень
- 12.5% узлов - всплывают на 2 уровня
- И т.д.

Сумма: 0.5×0 + 0.25×1 + 0.125×2 + ... = 1 ✓

---

### 4. Опишите как работает Heapsort

**Алгоритм Heapsort (in-place версия):**

**Фаза 1: Построение max-heap** O(n)
- Преобразуем исходный массив в max-heap
- Начинаем с последнего узла с потомками и идем к корню
- Каждый вызов sift-down восстанавливает свойство

**Фаза 2: Извлечение элементов** O(n log n)
- Максимум (корень) уже на месте
- Берем последний элемент, ставим в корень
- Уменьшаем размер кучи на 1
- Восстанавливаем свойство max-heap через sift-down
- Повторяем для оставшихся элементов

**Пример:** Сортируем [64, 34, 25, 12, 22, 11, 90]

```
Фаза 1 - Построение max-heap:
[64, 34, 25, 12, 22, 11, 90] → [90, 34, 64, 12, 22, 11, 25]

Фаза 2 - Извлечение:
[90, 34, 64, 12, 22, 11, 25] → поменяли 90 и 25, восстановили → [64, 34, 25, 12, 22, 11] | 90
[64, 34, 25, 12, 22, 11] → поменяли 64 и 11, восстановили → [34, 22, 25, 12] | 11, 90
... и т.д. ...
Результат: [11, 12, 22, 25, 34, 64, 90]
```

**Сложность:**
- Фаза 1: O(n)
- Фаза 2: O(n log n)
- **Итого: O(n log n)**

**Пространственная сложность (in-place):** O(1)

---

### 5. Почему кучу часто используют для приоритетной очереди?

**Требования приоритетной очереди:**
1. Быстрое добавление элемента: O(log n)
2. Быстрое извлечение элемента с наивысшим приоритетом: O(log n)
3. Быстрый просмотр следующего элемента: O(1)

**Альтернативные решения:**

| Структура | enqueue | dequeue | peek |
|-----------|---------|---------|------|
| **Куча** | **O(log n)** | **O(log n)** | **O(1)** | ← лучший выбор
| Отсортированный массив | O(n) | O(1) | O(1) |
| Неотсортированный массив | O(1) | O(n) | O(n) |
| Сбалансированное BST | O(log n) | O(log n) | O(1) |

**Почему куча идеальна:**

1. **Асимптотическая оптимальность** - O(log n) для основных операций
2. **Простая реализация** - array-based, без указателей
3. **Эффективная по памяти** - нет доп. структур
4. **Хорошая локальность кэша** - элементы подряд в памяти
5. **Практическое применение:**
   - Алгоритм Дейкстры: поиск кратчайшего пути
   - Алгоритм Прима: минимальное остовное дерево
   - ОС: планирование задач по приоритетам
   - Системы с событиями: обработка событий по приоритетам

---

## 🚀 Инструкция по запуску

### Требования
- Python 3.12+

### Запуск программы

1. **Запуск всех тестов и анализа:**
```bash
python analysis_heap.py
```

2. **Использование кучи в коде:**
```python
from heap import MinHeap, MaxHeap
from heapsort import heapsort, heapsort_inplace
from priority_queue import PriorityQueue

# Min-куча
heap = MinHeap()
heap.insert(50)
heap.insert(30)
print(heap.extract())  # 30

# Heapsort
array = [64, 34, 25, 12, 22, 11, 90]
heapsort_inplace(array)
print(array)  # [11, 12, 22, 25, 34, 64, 90]

# Приоритетная очередь
pq = PriorityQueue()
pq.enqueue("Задача", 5)
print(pq.dequeue())
```

3. **Запуск unit-тестов:**
```bash
python test_heap.py
```

---

## 📖 Использованная литература

1. **Кормен, Т., Лейзерсон, Ч., Ривест, Р., Штайн, К.** *Алгоритмы: построение и анализ*, 3-е издание. — М.: Вильямс, 2022. — Глава 6: Кучи.

2. **Скиена, С.** *Алгоритмы. Руководство по разработке*, 3-е издание. — СПб.: БХВ-Петербург, 2022. — Глава 4: Структуры данных.

3. **Петров, Ю.** *Программирование на Python* — онлайн-курс. URL: https://www.yuripetrov.ru/edu/python/index.html

4. **Bhargava, A. D.** *Grokking Algorithms: An Illustrated Guide for Programmers and Other Curious People*. Manning Publications, 2016. — Глава 8: Динамическое программирование.

---

## Примечания

- Все замеры проведены на одной машине
- Код соответствует стандарту PEP8
- Использована стратегия ветвления GitHub Flow
- Все файлы содержат подробные комментарии
- Массивы малых размеров в примерах для наглядности (реальные тесты использовали 1k-20k элементов)
